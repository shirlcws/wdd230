// initialize display element
const orderDisplay = document.querySelector("#orders");

let order = order.now()
//get the value in local Storage
let lastOrder = Number(window.localStorage.getItem("orders"));
// Using the Number( function ensures that if the storage item don't  exist, it will be converted
// on the if block condition.

// determine if this is the first order or display the number of orders)
if (lastOrder !== null) {
   console.log
}

